#!/bin/bash

NOW=`date "+%Y%m%d"`
SQL="backups/${NOW}_wiley.sql.bz2"
TAR="backups/${NOW}_wiley.tar.bz2"
FILES='.git .gitignore README WileyREADME attic bin docs etc lib public_html src'
EXCLUDE='lib/log/[12r]*'

pg_dump wiley | bzip2 -9 >$SQL
tar --exclude=$EXCLUDE -jcf $TAR $FILES

